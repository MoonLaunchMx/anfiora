# ANF-052 Playlist v2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Rediseñar el playlist (planner + página pública) con tabs, límite configurable de canciones, canciones de los novios y export para DJ (Excel/PDF/M3U).

**Architecture:** El planner adopta el patrón de mesa-regalos (zona fija header+stats+TabToggle, zona scrolleable por tab). La página pública abandona el hero negro y adopta el lenguaje visual de `app/mesa/[token]` (crema #FBF7F0 + teal). Dos columnas nuevas en Supabase distinguen canciones de novios (`is_host_pick`) y guardan el límite (`playlist_max_songs`).

**Tech Stack:** Next.js 16 App Router, Tailwind v4, Supabase (cliente anon browser), xlsx, jspdf + jspdf-autotable, qrcode.react, @dnd-kit.

**Spec:** `docs/superpowers/specs/2026-06-12-anf-052-playlist-v2-design.md`

**Sin tests** (regla del repo). Cada task termina en: `npx tsc --noEmit` o `npm run build` limpio + verificación manual + commit.

---

### Task 1: SQL para Diego (pre-requisito de pruebas locales)

**Files:**
- Create: `docs/superpowers/plans/anf-052-playlist-v2.sql`

- [ ] **Step 1: Crear el archivo SQL versionado**

```sql
-- ANF-052: limite configurable + canciones de los novios.
-- Inerte para prod: el codigo en main no referencia estas columnas.
-- DEFAULT false hace que las filas existentes lean false (PG11+ backfill virtual).

BEGIN;

ALTER TABLE event_settings ADD COLUMN IF NOT EXISTS playlist_max_songs INTEGER;
ALTER TABLE song_recommendations ADD COLUMN IF NOT EXISTS is_host_pick BOOLEAN DEFAULT false;

-- ANF-049 dejo el SELECT de anon en event_settings con grant por columna;
-- la pagina publica necesita leer la columna nueva:
GRANT SELECT (playlist_max_songs) ON event_settings TO anon;

COMMIT;
```

- [ ] **Step 2: Avisar a Diego que lo corra ANTES de probar en local** (el código nuevo selecciona estas columnas explícitamente; sin ellas la página pública daría 400).

- [ ] **Step 3: Commit**

```bash
git add docs/superpowers/plans/anf-052-playlist-v2.sql
git commit -m "docs(ANF-052): sql de columnas playlist_max_songs e is_host_pick"
```

---

### Task 2: Tipos

**Files:**
- Modify: `lib/types.ts` (type `EventSettings`, línea ~82)

- [ ] **Step 1: Agregar campo a EventSettings** (additivo, no rompe consumidores)

```ts
export type EventSettings = {
  // ...campos existentes sin cambio...
  playlist_token: string | null
  playlist_categories: string[] | null
  playlist_max_songs: number | null   // null = 3 (default), 0 = sin limite, 1..10 literal
  // ...resto sin cambio...
}
```

Helper exportado junto al type (lo usan planner y página pública):

```ts
// Resuelve el limite efectivo de canciones por invitado
export function resolveMaxSongs(value: number | null | undefined): number {
  if (value === 0) return Infinity
  if (!value || value < 0) return 3
  return value
}
```

- [ ] **Step 2: `npx tsc --noEmit` limpio**

- [ ] **Step 3: Commit** — `feat(ANF-052): playlist_max_songs en EventSettings + resolveMaxSongs`

---

### Task 3: Export DJ

**Files:**
- Create: `app/events/[id]/playlist/lib/exports.ts`

- [ ] **Step 1: Implementar las tres funciones de export**

Tipos y orden: novios primero (`is_host_pick`), luego invitados por `position`. Columnas: Orden, Canción, Artista, Etapa, De los novios, Veces pedida, Pedida por, Duración, Spotify, Notas.

```ts
import * as XLSX from 'xlsx'
import jsPDF from 'jspdf'
import autoTable from 'jspdf-autotable'

export type DjSong = {
  song_title: string
  artist: string
  category: string | null
  guest_name: string
  is_host_pick: boolean
  duration_ms: number | null
  spotify_url: string | null
  notes: string | null
}

export type DjExportData = {
  eventName: string
  eventDate: string | null
  songs: DjSong[]   // ya ordenadas: novios primero, luego position
}

function fmtDuration(ms: number | null): string {
  if (!ms) return ''
  const m = Math.floor(ms / 60000)
  const s = Math.floor((ms % 60000) / 1000)
  return `${m}:${String(s).padStart(2, '0')}`
}

function buildFileName(eventName: string, ext: string): string {
  const safe = eventName.replace(/[^a-z0-9]/gi, '_').toLowerCase()
  return `playlist-dj-${safe}.${ext}`
}

// cuenta repeticiones por titulo+artista (solo canciones de invitados)
function buildCounts(songs: DjSong[]): Map<string, number> {
  const map = new Map<string, number>()
  for (const s of songs) {
    if (s.is_host_pick) continue
    const key = `${s.song_title.toLowerCase().trim()}|${s.artist.toLowerCase().trim()}`
    map.set(key, (map.get(key) || 0) + 1)
  }
  return map
}

function buildRows(data: DjExportData): (string | number)[][] {
  const counts = buildCounts(data.songs)
  return data.songs.map((s, i) => {
    const key = `${s.song_title.toLowerCase().trim()}|${s.artist.toLowerCase().trim()}`
    return [
      i + 1,
      s.song_title,
      s.artist,
      s.category || '',
      s.is_host_pick ? 'Si' : 'No',
      s.is_host_pick ? '' : (counts.get(key) || 1),
      s.guest_name,
      fmtDuration(s.duration_ms),
      s.spotify_url || '',
      s.notes || '',
    ]
  })
}

const HEADERS = ['Orden', 'Cancion', 'Artista', 'Etapa', 'De los novios', 'Veces pedida', 'Pedida por', 'Duracion', 'Spotify', 'Notas']

export function exportDjToExcel(data: DjExportData) { /* libro 1 hoja, header con evento+fecha, ancho de columnas, XLSX.writeFile */ }
export function exportDjToPDF(data: DjExportData) { /* jsPDF landscape + autoTable, mismo head/body, doc.save */ }

export function exportDjToM3U(data: DjExportData) {
  const lines = ['#EXTM3U']
  for (const s of data.songs) {
    const secs = s.duration_ms ? Math.round(s.duration_ms / 1000) : -1
    lines.push(`#EXTINF:${secs},${s.artist} - ${s.song_title}`)
    lines.push(s.spotify_url || '')
  }
  const blob = new Blob([lines.join('\n')], { type: 'audio/x-mpegurl;charset=utf-8' })
  const url = URL.createObjectURL(blob)
  const a = document.createElement('a')
  a.href = url
  a.download = buildFileName(data.eventName, 'm3u8')
  a.click()
  URL.revokeObjectURL(url)
}
```

(Excel/PDF: copiar la estructura de `app/events/[id]/pagos/lib/exports.ts` — mismo stack y convenciones.)

- [ ] **Step 2: `npx tsc --noEmit` limpio**

- [ ] **Step 3: Commit** — `feat(ANF-052): export DJ excel/pdf/m3u`

---

### Task 4: Planner — tabs Playlist / Nuestras canciones / Configuración

**Files:**
- Modify: `app/events/[id]/playlist/page.tsx` (reescritura del layout; SongRow/DragCard/lógica dnd se conservan)
- Create: `app/events/[id]/playlist/HostSongsTab.tsx`

- [ ] **Step 1: HostSongsTab** — buscador Spotify (mismo debounce/fetch a `/api/spotify/search` que la página pública) + lista de canciones `is_host_pick`. Insert:

```ts
await supabase.from('song_recommendations').insert({
  event_id: id,
  guest_name: hostLabel,        // "host_name & host_name_2" o 'Los novios'
  song_title: track.title,
  artist: track.artist,
  category: null,
  spotify_url: track.spotify_url,
  thumbnail: track.thumbnail,
  preview_url: track.preview_url,
  duration_ms: track.duration_ms,
  is_host_pick: true,
})
```

Props: `{ eventId, hostLabel, songs, onAdd, onRemove }` — el estado vive en page.tsx (una sola fuente: `allSongs`).

- [ ] **Step 2: page.tsx** — reestructura al patrón mesa-regalos:
  - `loadData` agrega: select de `events (host_name, host_name_2, name, event_date)` para hostLabel y exports; `playlist_max_songs` al select de event_settings; `is_host_pick` viene en el `select('*')` existente.
  - `guestSongs = allSongs.filter(s => !s.is_host_pick)` / `hostSongs = allSongs.filter(s => s.is_host_pick)`. Stats, top5, filtros y dnd operan sobre `guestSongs`.
  - Zona fija: header (título + stats con StatsCollapse en mobile) + `<TabToggle tabs={TABS} active={tab} onChange={setTab} />` con `TABS = [{key:'playlist', label:'Playlist', icon: Music, badge: guestSongs.length}, {key:'nuestras', label:'Nuestras canciones', shortLabel:'Nuestras', icon: Heart, badge: hostSongs.length}, {key:'config', label:'Configuración', shortLabel:'Config', icon: Settings}]`.
  - Tab playlist: toolbar (search + filtro etapa + botón Descargar para DJ con dropdown Excel/PDF/M3U) + SongList actual. Un solo layout responsive (desaparece el doble árbol MOBILE/DESKTOP actual).
  - Tab nuestras: `<HostSongsTab .../>`.
  - Tab config: link + copiar + QR (`<QRCodeCanvas value={playlistUrl} size={140} />` como album) + StageManager + selector de límite:

```tsx
const LIMIT_OPTIONS = [1, 3, 5, 10, 0]  // 0 = sin limite
// pills estilo mesa-regalos; label: n === 0 ? 'Sin límite' : String(n)
const saveMaxSongs = async (n: number) => {
  setMaxSongs(n)
  await supabase.from('event_settings').update({ playlist_max_songs: n }).eq('event_id', id)
}
// valor activo: maxSongs ?? 3
```

- [ ] **Step 3: `npm run build` limpio + verificación manual** (dev server: tabs cambian, agregar canción de novios aparece en tab y badge, límite persiste tras reload, export descarga 3 archivos).

- [ ] **Step 4: Commit** — `feat(ANF-052): planner playlist con tabs, nuestras canciones y export DJ`

---

### Task 5: Página pública — restyle + novios + límite dinámico

**Files:**
- Modify: `app/playlist/[token]/page.tsx` (reescritura visual; lógica de búsqueda/insert/preview se conserva)

- [ ] **Step 1: Datos** —
  - Query 1: `.select('event_id, playlist_categories, playlist_max_songs')`.
  - Query 2 (events): agregar `host_name, host_name_2`.
  - Query 3 (songs): agregar `is_host_pick` al select.
  - `const maxSongs = resolveMaxSongs(settingsData.playlist_max_songs)` — reemplaza TODOS los `3` hardcodeados (líneas 161, 182, 221, 229, 267, texto "Hasta 3", "0:30 ..." no, contador `x/3`, botón `(x de 3)`).
  - Conteos para límite: agregar `.eq('is_host_pick', false)` a los counts por guest_name (filas legacy leen false por el DEFAULT).
  - `done` solo cuando `Number.isFinite(maxSongs) && count >= maxSongs`. Con `Infinity`: ocultar contador y "Hasta N".

- [ ] **Step 2: Restyle al lenguaje mesa pública** (referencia: `app/mesa/[token]/page.tsx`):
  - Loading/notFound: fondo `#FBF7F0`, spinner teal (notFound conserva el copy actual de link inválido).
  - Hero NO sticky (scroll normal, como mesa): label "Playlist del evento" uppercase tracking `text-[#aaa]` Josefin, título `host_name & host_name_2` (fallback `event.name`) Josefin 4xl/5xl `#1D1E20`, fecha y venue, mensaje corto "Ayúdanos a armar la playlist..." `text-[#666]`.
  - Sección novios (si `hostSongs.length > 0`): título "Las canciones de los novios" + cards blancas borde teal suave, play preview reutilizado.
  - Form (nombre / etapas / búsqueda): mismos componentes, inputs `focus:border-[#48C9B0]`, botones `bg-[#48C9B0] hover:bg-[#3aa896]` (reemplaza `bg-[#111]`), pills de etapa estilo filtro de mesa (teal activo).
  - Lista general: cards blancas `rounded-2xl border-[#eee4d6]`, contador de canciones, preview con barra teal.
  - Footer: `border-[#eee4d6] bg-[#F5EFE3]`, "Anfiora" Josefin + subtítulo "La playlist de tu evento".
  - Mini player móvil se conserva (estilo claro).

- [ ] **Step 3: Verificación manual** en incógnito: hero crema con nombres, sección novios, límite N correcto, agregar canción funciona, M3U/Excel/PDF reflejan la nueva canción tras reload del planner.

- [ ] **Step 4: `npm run build` limpio**

- [ ] **Step 5: Commit** — `feat(ANF-052): pagina publica playlist con estilo mesa, canciones de novios y limite dinamico`

---

### Task 6: Cierre

- [ ] **Step 1:** `npm run build` final limpio.
- [ ] **Step 2:** Levantar `npm run dev` y entregar a Diego URLs para validar (planner + link público en incógnito).
- [ ] **Step 3:** NO push, NO merge — Diego valida primero en local (regla de la sesión).

## Self-review

- Spec coverage: datos (T1/T2), tabs planner (T4), nuestras canciones (T4), config con QR y límite (T4), pública restyle+novios+límite (T5), export DJ (T3/T4). Fuera de alcance (hardening collaborators) no se toca. ✓
- Sin TBDs; los dos cuerpos Excel/PDF referencian el patrón exacto de pagos/lib/exports.ts a copiar. ✓
- Consistencia de tipos: `resolveMaxSongs` definido en T2 y usado en T5; `DjSong.is_host_pick` definido en T3 y poblado en T4. ✓
